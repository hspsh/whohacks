version: '2'
services:
  web:
    build: .
    restart: always
    ports:
      # use 127.0.0.1:8000:8000
      - "8000:8000"
    volumes:
      - db:/data
      - /etc/localtime:/etc/localtime:ro

volumes:
  db:
