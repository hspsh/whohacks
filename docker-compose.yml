version: "3"
services:
  web:
    build: .
    restart: unless-stopped
    environment:
      # you should change secret key
      - SECRET_KEY=example123
      - APP_DB_URL="postgresql://whohacks:S3cret@localhost:5432/whohacks"
      - OAUTH_OPENID=http://oauth2:8080/issuer1/.well-known/openid-configuration

    ports:
      # use 127.0.0.1:8000:8000
      - "8000:8000"
    volumes:
      - ./data:/sqlite
      - /etc/localtime:/etc/localtime:ro
    depends_on:
      - postgres

  postgres:
    image: postgres:latest
    restart: unless-stopped
    ports:
      - 5432:5432
    volumes:
      - database_postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=S3cret
      - POSTGRES_USER=whohacks
      - POSTGRES_DB=whohacks

volumes:
  database_postgres:
    driver: local
